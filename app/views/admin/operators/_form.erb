<%= form_for [:admin, operator], html: { class: 'form-horizontal' } do |f| %>
  <%= f.text_field :name %>
  <%= f.text_field :email %>
  <%= f.select :sector_id, Sector.pluck(:name,:id) %>
  <%= f.submit "Create" %>
<% end %>