<%= link_to "Nova categoria", new_admin_category_path %>

<% @categories.each do |category| %>
	<%= category.name %> 
	| <%= link_to "Editar", edit_admin_category_path(category) %> 
	| <%= link_to "Deletar", admin_category_path(category) , data: { method: :delete} %> <br>
<% end %>